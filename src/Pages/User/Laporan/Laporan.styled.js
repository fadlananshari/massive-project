import styled from "styled-components";

const LaporanStyled = styled.div`
  .bg-shadow {
    box-shadow: 0px 4px 4px 0px rgba(0, 0, 0, 0.25);
  }

  .text-blue {
    color: #1e90ff;
  }

  .card-custom {
    background: #e3ecff;

    box-shadow: 0px 4px 4px 0px rgba(0, 0, 0, 0.25);
  }

  .text-justify {
    text-align: justify;
  }

  .btn-danger {
    background: #ff1e1e;
  }

  .btn-custom {
    background: #1e90ff;
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 1.5rem;
    height: 5rem;
  }
`;

export default LaporanStyled;
