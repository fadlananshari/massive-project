import styled from "styled-components";

const ProgramStudiStyled = styled.div`
  .bg-biru {
    background: #edf2fb;
    height: auto;
    padding-bottom: 5rem;
  }

  .text-title {
    color: #1e90ff;
    font-family: Roboto Serif;
    font-size: 3.75rem;
    font-style: normal;
    font-weight: 700;
    line-height: normal;
  }

  .text-desc {
    color: #000;
    font-family: Roboto Serif;
    font-size: 1.5625rem;
    font-style: normal;
    font-weight: 500;
    line-height: normal;
  }
`;

export default ProgramStudiStyled;
