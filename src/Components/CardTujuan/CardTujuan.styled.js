import styled from "styled-components";

const CardTujuanStyled = styled.div`
  height: 100% !important;

  .card-text {
    color: rgba(0, 0, 0, 0.8);
    text-align: center;
    font-family: Roboto Serif;
    font-size: 1rem;
    font-style: normal;
    font-weight: 400;
  }
`;

export default CardTujuanStyled;
